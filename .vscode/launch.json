{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/program.exe"
            // "program": "${workspaceFolder}/${workspaceFolderBasename}.exe"
            // "program": "${fileWorkspaceFolder}/${workspaceFolderBasename}.exe"
             , 
            "cwd": "${workspaceRoot}",
            "externalConsole": true,
            "MIMode": "gdb",
            "preLaunchTask": "Build Debug",
            "environment": [
            ],
            "args": [
            ]
        }
    ]
}